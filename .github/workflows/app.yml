name: App

on:
  push:
    branches: [main]
    paths:
      - "src/**"
      - "tests/**"
      - "DotGlasses.sln"
      - ".github/workflows/app.yml"
  pull_request:
    branches: [main]
    paths:
      - "src/**"
      - "tests/**"
      - "DotGlasses.sln"
      - ".github/workflows/app.yml"
  # Manual escape hatch for triggering a run without a matching src/**/tests/** push — e.g. to
  # validate the deploy.yml workflow_run chain end-to-end. Deliberately not added to deploy.yml
  # itself, which would let a deploy fire without a build/test run ever happening.
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Restore
        run: dotnet restore DotGlasses.sln

      - name: Build
        run: dotnet build DotGlasses.sln --no-restore --configuration Release

      - name: Test
        run: dotnet test DotGlasses.sln --no-build --configuration Release --logger "trx;LogFileName=test-results.trx"

      - name: Publish test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: "**/TestResults/*.trx"
