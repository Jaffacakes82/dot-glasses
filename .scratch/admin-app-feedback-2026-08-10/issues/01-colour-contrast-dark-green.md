# 01 — Colour contrast — dark green background

Status: done (implemented 2026-09-03, PR: fix/copy-contrast-and-org-terminology)
Blocked by: None — can start immediately
Category: bug

Source: Admin & App Feedback, 2026-08-10, "General"

Black text on a dark green background is not sufficiently visible. Suggested fix: switch to an
orange background, or keep dark green but switch text to off-white/white.

Category candidate: bug (accessibility/contrast).

## Comments

> *This was generated by AI during triage.*

## Agent Brief

**Category:** bug
**Summary:** Fix insufficient text/background contrast on the dark-green UI surface

**Current behavior:**
Black text is rendered on a dark-green background, which is not sufficiently legible.

**Desired behavior:**
The surface meets a reasonable contrast standard (e.g. WCAG AA, ~4.5:1 for normal text) — either
by switching the background to orange, or keeping dark green and switching the text to
off-white/white. Either fix resolves the underlying legibility defect.

**Key interfaces:**
- The shared design-token file(s) (`wwwroot/css/dot-glasses.css`) — this file is hand-duplicated
  across the Admin Portal and Field App per CLAUDE.md; if the token is shared, update both copies
  identically.

**Acceptance criteria:**
- [ ] The affected surface's text meets at least WCAG AA contrast against its background.
- [ ] Both `dot-glasses.css` copies are updated identically if the token is shared between them.
- [ ] No unrelated surface using the same token changes unintentionally.

**Out of scope:**
- A full colour-palette audit beyond this specific reported surface.
