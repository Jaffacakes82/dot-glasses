# 12 — Hard case checkbox — wording and layout

Source: Admin & App Feedback, 2026-08-10, "Leads"

The hard-case checkbox shouldn't be presented as an imperative question ("Add a hard case"),
which caused confusion about what was being asked. Suggested rewording: "Did this person buy a
hard case? (click for yes)". Also consider moving the checkbox to the right of the question text
so the user reads left-to-right before clicking.

Category candidate: enhancement, small — good ready-for-agent candidate once wording is signed
off.

Status: done (implemented 2026-09-03, PR: fix/field-app-consultation-form-ux)
Blocked by: None — can start immediately
Category: enhancement

## Comments

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Reword and reposition the hard-case checkbox on the consultation form

**Current behavior:**
The hard-case checkbox is presented as an imperative instruction ("Add a hard case"), which
reviewers found confusing about what state the checkbox actually represented. The checkbox
currently appears before its label text.

**Desired behavior:**
The label reads as a yes/no question about what happened, e.g. "Did this person buy a hard case?
(click for yes)," and the checkbox control appears after its label text, so the user reads the
question before reaching the control.

**Key interfaces:**
- The hard-case field's label copy and layout in the consultation form.

**Acceptance criteria:**
- [ ] The hard-case field's label reads as a question about what happened, not an instruction.
- [ ] The checkbox control appears after (to the right of, in reading order) its label text.
- [ ] The field's underlying saved data/behavior is unchanged — copy/layout only.

**Out of scope:**
- Changing where in the form this field appears or what triggers it to show.
