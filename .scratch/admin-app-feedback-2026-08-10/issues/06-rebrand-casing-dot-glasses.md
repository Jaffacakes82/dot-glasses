# 06 — Rebrand casing: "DOT" → "Dot Glasses"

Status: done (implemented 2026-09-03, PR: fix/copy-contrast-and-org-terminology)
Blocked by: None — can start immediately
Category: bug

Source: Admin & App Feedback, 2026-08-10, "Field Distribution App"

Update spelling/casing to "Dot Glasses" rather than all-caps "DOT" everywhere it appears in the
UI copy.

Category candidate: bug (copy fix) — likely small and mechanical, good ready-for-agent
candidate once someone greps for the current casing across `Web`/`App`.

## Comments

> *This was generated by AI during triage.*

Confirmed small and mechanical — 16 file:line hits found across both UI projects, all
straightforward copy changes, plus two adjacent surfaces worth bundling into the same pass.

## Agent Brief

**Category:** bug (copy fix)
**Summary:** Replace all-caps "DOT" brand references with "Dot Glasses" in user-facing UI copy
across the Admin Portal and Field App

**Current behavior:**
User-facing text — page titles, the nav/brand header, login and access-denied screens, and body
copy referencing the company — reads the brand name as all-caps "DOT Glasses" in both the Admin
Portal and Field App. The same all-caps form also appears in the generated Swagger API document
title and in outbound invitation email copy.

**Desired behavior:**
All of the above locations read "Dot Glasses" (title case) instead. The PascalCase code
identifier `DotGlasses` (namespaces, project names, C# type names) is unrelated brand-adjacent
naming and must not be touched by this change.

**Key interfaces:**
- No type/interface changes — this is string-literal copy changes only, in Razor views/
  components across the Admin Portal and Field App projects, plus the Swagger configuration's
  document title and the invitation email template's subject/body text.

**Acceptance criteria:**
- [ ] A search for the literal all-caps brand string "DOT" used as UI copy (page titles, nav/
      brand headers, login/access-denied screens, body copy referencing the company) across both
      UI projects returns no remaining matches.
- [ ] The Swagger API document title reflects the same "Dot Glasses" casing.
- [ ] The outbound invitation email subject/body reflects the same "Dot Glasses" casing.
- [ ] No change to any PascalCase code identifier (`DotGlasses.Web`, `DotGlasses.App`, C#
      namespaces/types) or to any database seed data.

**Out of scope:**
- Renaming the PWA manifest's `name`/`short_name` (currently `"DotGlasses.App"`, PascalCase, not
  the all-caps "DOT" this ticket targets) — a separate naming decision.
- Renaming the seeded "DOT Glasses International" organisation record — that's data, not
  template copy, and changing it renames a live database row; a separate decision if wanted.
- Any C# namespace or identifier renames.
