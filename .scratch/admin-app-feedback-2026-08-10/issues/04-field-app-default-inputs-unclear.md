# 04 — Field App: unclear "default inputs" feedback

Source: Admin & App Feedback, 2026-08-10, "Field Distribution App"

Raw note under a "Screenshots" heading: "Don't have default inputs". No screenshots were
attached to the extracted text and the intent isn't clear from the sentence alone — could mean
"fields shouldn't have pre-filled default values" or something else entirely.

Category candidate: needs-info — ask the reporter what this refers to (which screen, which
field, and what "default input" means here).

## Settled design (2026-09-03)

Clarified: "don't have default inputs" means input boxes in the Field App shouldn't show
placeholder text. Scope is the whole Field App, not one specific field/screen — audit every
`<input>`/text field across `DotGlasses.App` (primarily `ConsultationForm.razor` and any other
form pages) and remove placeholder text that could read as a pre-filled real value, leaving
fields genuinely blank until the technician types into them.

Status: done (implemented 2026-09-03, PR: fix/field-app-consultation-form-ux)
Blocked by: None — can start immediately
Category: enhancement

## Comments

> *This was generated by AI during triage.*

## Triage Notes

**What we've established so far:**

- Nothing beyond the raw sentence — no screenshots came through in the extracted text, and the
  surrounding context doesn't disambiguate the intent.

**What we still need from you (@reporter):**

- Which screen and field does "don't have default inputs" refer to?
- What did you mean by "default input" here — a field pre-filled with a value that shouldn't be
  pre-filled, a placeholder that looks like a real value, or something else?

> *2026-09-03.* Reporter clarified: means placeholder text in input boxes. Treating as a
> Field-App-wide audit rather than a single field — see Settled design above.
