# 13 — Reference data: add Lens Type category

Source: Admin & App Feedback, 2026-08-10, "Users" section

Add "lens type" as an option on Reference Data. Starting options: Bifocal, Progressive — with a
default "other" option offering free text input on the app (consistent with the existing
`IsOtherOption` pattern used by other reference-data categories).

Category candidate: enhancement — likely a new `ReferenceDataCategory` value; feeds ticket 14
(the old "issue 30" this once fed was later parked as `wontfix`).

Status: done (implemented 2026-09-03, PR: feat/reference-data-and-lens-type)
Blocked by: None — can start immediately
Category: enhancement

## Comments

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Add "Lens Type" as a new Reference Data category

**Current behavior:**
`ReferenceDataCategory` doesn't include a Lens Type category — there is no admin-managed
dropdown for lens type (Bifocal/Progressive/Other).

**Desired behavior:**
A new `ReferenceDataCategory` value for Lens Type exists, seeded with Bifocal and Progressive
plus a default `IsOtherOption` item offering free text — consistent with every other
reference-data category's existing pattern (admin-manageable via the Reference Data screen,
retirable via `IsActive = false`, at most one active `IsOtherOption` item per category).

**Key interfaces:**
- `ReferenceDataCategory` — add the new value.
- Seed data for the two starting options plus the Other option. No new mechanism is needed; this
  reuses the existing generic `ReferenceDataItem` infrastructure.

**Acceptance criteria:**
- [ ] A new Lens Type category appears in the Reference Data admin screen, editable the same way
      every other category is.
- [ ] It's seeded with Bifocal, Progressive, and an Other option with free text.
- [ ] DGI admins can add/retire Lens Type options the same way as any other reference-data
      category, with the same single-active-Other-option constraint enforced.

**Out of scope:**
- Wiring this category into any consuming screen (ticket 14 in this feedback batch; the old
  "issue 30" this once also fed was later parked as `wontfix`) — this
  ticket only adds the category itself.
