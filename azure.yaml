# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: dot-glasses
services:
  app:
    language: dotnet
    project: ./src/DotGlasses.AppHost/DotGlasses.AppHost.csproj
    host: containerapp
