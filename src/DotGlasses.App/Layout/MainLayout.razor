@inherits LayoutComponentBase

@* No persistent chrome — per the design mockups, Field App screens are full-bleed and use
   their own TopStrip/back-button navigation instead of a permanent sidebar/nav bar. *@
@Body
