<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <!-- Contracts only, deliberately: DotGlasses.App (Blazor WASM) references this project, so
         anything Rules drags in it drags in too. No Domain, no Application, no Infrastructure,
         no EF Core, no ASP.NET — see ADR-0002 and CLAUDE.md's App-reference rule. -->
    <ProjectReference Include="..\DotGlasses.Contracts\DotGlasses.Contracts.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
